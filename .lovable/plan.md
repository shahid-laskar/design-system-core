# Product detail page

## Build
- Bring the approved Sukoon House tokens and canonical brand primitives into this project unchanged.
- Add the shared Sukoon House header and footer needed to present the page in its intended system.
- Create `/products/$productId` with realistic mock data for The Stillness Set and graceful product-ID fallback behavior.
- Build a responsive image gallery, sticky purchase panel, variant and quantity controls, Add to Bag feedback, product-detail accordions, guarantee notes, one bundle, and one complementary recommendation.
- Keep all purchasing interactions local; no checkout or backend work.

## Technical details
- Reuse `PageContainer`, `Eyebrow`, `SectionHeading`, and the existing Button/Accordion primitives.
- Copy the approved local product imagery into this project rather than linking externally.
- Add route-specific title, description, Open Graph, and Twitter metadata.
- Preserve semantic design tokens and verify desktop/mobile rendering, interactions, and preview diagnostics.
